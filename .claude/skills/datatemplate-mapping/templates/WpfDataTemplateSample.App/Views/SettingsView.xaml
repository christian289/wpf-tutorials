<UserControl x:Class="WpfDataTemplateSample.App.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewmodels="clr-namespace:WpfDataTemplateSample.ViewModels;assembly=WpfDataTemplateSample.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=viewmodels:SettingsViewModel}">

    <Grid Background="#F5F5DC">
        <StackPanel HorizontalAlignment="Center"
                    VerticalAlignment="Center">
            <TextBlock Margin="0,0,0,20"
                       HorizontalAlignment="Center"
                       FontSize="32"
                       FontWeight="Bold"
                       Foreground="#8B4513"
                       Text="{Binding Title}" />

            <Border MaxWidth="600"
                    Padding="30"
                    Background="White"
                    BorderBrush="#D2691E"
                    BorderThickness="2"
                    CornerRadius="10">
                <StackPanel>
                    <CheckBox Content="Enable Dark Mode"
                              IsChecked="{Binding IsDarkModeEnabled}"
                              FontSize="14"
                              Margin="0,5" />
                    <CheckBox Content="Enable Notifications"
                              IsChecked="{Binding IsNotificationsEnabled}"
                              FontSize="14"
                              Margin="0,5" />
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>
